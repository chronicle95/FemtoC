/* File made to test the compiler */

char C = 33;
int q = 0;

int copy(char *d, char *s, int n)
{
    while (n > 0)
    {
        *d = *s;
        d = d + 1;
        s = s + 1;
        n = n - 1;
    }
}

int f2(int x)
{
    char arr[10];
    int j = 0;
    if (x < 7)
    {
        return x * 2 + q - j;
    }
    else
    {
        return x + 2;
    }
}

int f1(int a, int b)
{
    return a + b - f2(a);
}

int main()
{
    C = C + 5;
    copy (&q, &C, 1);
    while (C > 0)
    {
        q = *&q + 3;
        C = C - 1;
    }
    return 8 * f1(1, q);
}
