/* File made to test the compiler */

char C = 33;
int q = 0;

int f2(int x)
{
    char arr[10];
    int j = 0;
    if (x < 7)
    {
        return x * 2 + q - j;
    }
    else
    {
        return x + 2;
    }
}

int f1(int a, int b)
{
    return a + b - f2(a);
}

int main()
{
    C = C + 5;
    while (C > 0)
    {
        q = *&q + 3;
        C = C - 1;
    }
    return 8 * f1(1, q);
}
